import { ImageContainer } from '../../components/imageContainer';
import { Tab, Tabs } from 'nextra-theme-docs';
import UITextTypescriptSDK from '../../snippets/typescript-sdk/ui-text.mdx';
import UITextGraphQL from '../../snippets/graphql/ui-text.mdx';

# Text

<ImageContainer withBorder={true}>![Example text](/images/ui-components/text.png)</ImageContainer>

The text component has the following properties:

- `text`: the text. Can include a subset of markdown (bold, italic, and links).
- `textSize` (optional): one of `S`, `M`, `L`, defaults to `M`
- `textColor` (optional): one of `NORMAL`, `MUTED`, `SUCCESS`, `WARNING`, `ERROR`, defaults to `NORMAL`

For example:

<Tabs items={[ 'Typescript SDK', 'GraphQL']}>
    <Tab>
        <UITextTypescriptSDK />
    </Tab>

    <Tab>
        <UITextGraphQL />
    </Tab>

</Tabs>

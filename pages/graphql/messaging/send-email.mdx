import { Tab, Tabs } from 'nextra-theme-docs';

import SendEmailTypescriptSDK from '../../../snippets/typescript-sdk/send-email.mdx';
import SendEmailGraphQL from '../../../snippets/graphql/send-email.mdx';

# Send emails

You can send new outbound emails to customers with the `sendNewEmail` API. This is useful for proactively reaching out about issues.

<Tabs items={[ 'Typescript SDK', 'GraphQL']}>
    <Tab>
        <SendEmailTypescriptSDK />

    </Tab>

    <Tab>
        <SendEmailGraphQL />
    </Tab>

</Tabs>

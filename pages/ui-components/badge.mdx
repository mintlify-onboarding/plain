import { ImageContainer } from '../../components/imageContainer';
import { Tab, Tabs } from 'nextra-theme-docs';
import UIBadgeTypescriptSDK from '../../snippets/typescript-sdk/ui-badge.mdx';
import UIBadgeGraphQL from '../../snippets/graphql/ui-badge.mdx';

# Badge

Useful for statuses or when you need to attract attention to something.

<ImageContainer withBorder={true}>
  ![Example badges](/images/ui-components/badge.png)
</ImageContainer>

A badge has the following properties:

- `badgeLabel`: the text that should be displayed on the badge
- `badgeColor`: one of `GREY`, `GREEN`, `YELLOW`, `RED`, `BLUE`

For example:

<Tabs items={[ 'Typescript SDK', 'GraphQL']}>
    <Tab>
        <UIBadgeTypescriptSDK />
    </Tab>

    <Tab>
        <UIBadgeGraphQL />
    </Tab>

</Tabs>
